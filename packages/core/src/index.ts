/**
 * @oram/core public entry point. Currently exposes only the `workflow` module (StepId, Workflow,
 * ENGINEERING_WORKFLOW). This package is intentionally minimal: pure, dependency-free declarative types,
 * with no engine implementations and no imports of any other @oram/* package.
 */
export * from "./workflow";
