{
  "name": "@oram/runtime",
  "version": "0.0.0",
  "private": true,
  "description": "The ORAM Core Runtime: Lifecycle, EventBus, ArtifactStore, ProviderRegistry, Logger (see docs/ORAM_SPECIFICATION_v1.md Section 5).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@oram/core": "*",
    "@oram/events": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  },
  "//devDependencies": "Declared range predates verified toolchain install (typescript 7.0.2 actually used for this PR's test run) -- see this PR's final report, Risks/technical debt.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
